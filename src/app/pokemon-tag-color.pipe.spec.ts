import { PokemonTagColorPipe } from './pokemon-tag-color.pipe';

describe('PokemonTagColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PokemonTagColorPipe();
    expect(pipe).toBeTruthy();
  });
});
